# Exact-exposure admission, 2026-09-08

ADMITTED for bounded numerical reproduction and semantic diagnostics only. The previous Pulse and CellML discovery HOLD records remain historical. This admission applies only to exposure `b503501533abcf0e70786789f08cb902`, revision `2e0e7ccc682bbf326829b3d78e3137ebdf8dbca0`; retrieved XML, generated Python, metadata and documentation are hashed in manifest.json. Model code is shipped unchanged. The full generated code was read before execution. No downloaded scripts, external imports from XML or arbitrary uploaded code execute.

Catherine Lloyd's CellML encoding of Alona Ben-Tal's work explicitly carries CC BY 3.0 on this exposure's metadata. Attribution, source link and unchanged-code notice are included in the package. XML and generated Python are redistributed as model artifacts; the source article and figures are not. Exact files and notice are retained. See [license](https://creativecommons.org/licenses/by/3.0/) and [exact metadata](https://models.physiomeproject.org/exposure/b503501533abcf0e70786789f08cb902/bental_2006.cellml/cmeta). Browser fetch returned 403; direct HTTPS retrieval succeeded and retained source bytes. No license is transferred to another revision.

The documentation explicitly says this is a combination of equations 2,7,9,10,11,12,13,16,20 and does not correspond to any one of the paper's four models. The paper is Ben-Tal (2006), J Theor Biol 238(2):474–495, DOI 10.1016/j.jtbi.2005.06.005. Full article not obtained. We reproduce the publicly published software exposure, not paper figures. A curator's historical validation statement is not independent reference data. No independent empirical trajectory has been obtained.

The source-generated default solver is SciPy ode VODE/BDF with rtol=atol=1e-6, max_step=1, 500 times from 0 to 10 seconds. This is an independent numerical algorithm compared with Radau, but shares equations and is not an independent physiological reference. Every candidate retains that comparison. Extra convergence and independently derived checks are required in the research packages.

Supported numerical scope: initial state unchanged, 0..10 seconds; only lungMechanics R scaled 0.9..1.1. This is a software sensitivity neighbourhood with no probabilistic or physiological population interpretation. Constants with duplicated names are not silently tied together. Source E and z unit labels require review. Pleural pressure derivative appears inconsistent with analytic differentiation; it remains unchanged, visible and testable. No event mapping supports care-to-physiology coupling; scheduling is independent synthetic engineering research.

Production dependencies are project-local NumPy 2.2.6 (BSD), SciPy 1.15.3 (BSD plus bundled notices), standard Python. One numerical worker per experiment; BLAS threads set to one, wall timeout ≤120 seconds, trajectory size ≤2001 samples, finite inputs, CPU/file size caps. Same-user artifact protection is convention, not security isolation. Qualified interpretation and external human reproduction remain pending.
