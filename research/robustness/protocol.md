# Frozen protocol: solver versus local resistance sensitivity

Frozen 2026-09-08 before any confirmation run. Investigator runtime verified from this session's turn_context: model gpt-6-astra, reasoning medium. Read CONTRACT.md and ADMISSION.md. Prior exploratory bundle 0410cadedc24487b961116accb4afa02 informed scope only and is excluded from confirmation.

Question: in the exact unchanged Ben-Tal combination exposure, which differences reflect solver settings and which remain under a local R change? All integrations use the frozen investigation pyz, unchanged initial states, 10 seconds, 2001 uniformly spaced output times, a single sequential worker, pinned project Python/NumPy/SciPy. R scale 0.9,1,1.1 is an arbitrary admitted numerical neighbourhood, not a population distribution. No events, empirical data or care coupling.

Schedule (18 runs): nine controls comprising Radau, BDF and DOP853 at each R scale with rtol=1e-11, atol=1e-13, max_step=.01; baseline Radau at 1e-8/1e-10/.05; four Radau scalar-tolerance probes at rtol=1e-8, max_step=1 and atol=1e-3,1e-6,1e-10,1e-13; source-vode at 1e-6/1e-6/1 and 1e-11/1e-13/.01; RK45 at 1e-8/1e-10/.05; Radau convergence refinement at 1e-11/1e-13/.005. All probes use R=1. Actual source-generated VODE uses requested settings for the candidate; the platform also retains its fixed source-default comparator.

Primary comparators: the pointwise minimum/maximum of the three high-accuracy controls at each R. Reference anchor is high-accuracy Radau, not the source-default VODE. Numerical envelope = observed cross-algorithm range, not a rigorous error bound or statistical confidence interval. Report per-state maximum absolute disagreement, RMS disagreement and final value. Source units preserved (including unresolved z label). Report baseline-to-perturbed trajectory maximum absolute change and final signed change. Model response is numerically separated if maximum change exceeds 10 times the sum of baseline and perturbed maximum control-envelope widths and those widths pass convergence thresholds. Thresholds, engineering choices without clinical meaning: V_A 1e-6 litre; P_A,p_o,p_c 1e-4 mmHg; f_o,f_c 1e-7 dimensionless; z 1e-8 in unresolved source-labelled units. A state is converged only if both control width and Radau refinement difference pass its threshold. Failed convergence is reported without declaring reference truth.

Scalar tolerance analysis: report z initial magnitude, atol/(rtol*abs(z0)), maximum absolute and pointwise relative z differences (initial sample excluded). Uniform output sampling may miss between-sample maxima and very early transients; no claim of a full continuous-time error bound. Statewise tolerance vectors are not supported, and no substitute solver is introduced. Do not infer an observed z error solely from the local error scaling formula.

Stopping: execute exactly these 18 planned attempts once, sequentially, timeout 60 seconds each, no automatic retries or result-driven grid. Hard budget 30 platform run calls; unused capacity is not permission for exploratory expansion. Retain every command, stdout/stderr, failed result and elapsed cost; a failed control makes associated comparisons unavailable. Record source/build/runtime hashes. Analyze with independent CSV processing; do not rerun equations outside the platform. A rerun is a new timestamped campaign preserving original attempts.

Primary sources: [SciPy 1.15.3 solve_ivp](https://docs.scipy.org/doc/scipy-1.15.3/reference/generated/scipy.integrate.solve_ivp.html) documents the algorithms and local error weighting atol+rtol*abs(y); heterogeneous state magnitudes motivate absolute-tolerance probes. [Exact CellML exposure](https://models.physiomeproject.org/exposure/b503501533abcf0e70786789f08cb902/bental_2006.cellml/view), retained locally in docs/benchmarks/bental-2006, defines the equations and limits correspondence to the paper. [Ben-Tal 2006](https://doi.org/10.1016/j.jtbi.2005.06.005) is bibliographic context only; article not obtained. Same-equation algorithm agreement cannot establish physiological validity. No novelty, patient outcome, therapeutic benefit or human review claims.
